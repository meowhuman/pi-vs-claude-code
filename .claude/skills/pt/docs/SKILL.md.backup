---
name: Polymarket Trader
description: 自動執行 Polymarket 交易、查看持倉、搜尋市場。Use when user mentions polymarket trading, buying/selling positions, prediction markets, checking portfolio, market search, or wants to bet on events. Works with Polygon network and USDC.
---

# Polymarket Trader

自動化 Polymarket 交易 skill - 連接 Phantom 錢包，執行買賣交易，管理持倉。

## Prerequisites

**必須安裝的 Python packages**:
```bash
pip install web3 requests python-dotenv eth-account
```

**環境變數設定** (在 `.env` 文件):
- `POLYGON_PRIVATE_KEY` - 你的 Polygon 私鑰
- `POLYGON_RPC_URL` - Polygon RPC endpoint (預設: https://polygon-rpc.com)
- `WALLET_ADDRESS` - 你的錢包地址
- `MAX_AUTO_AMOUNT` - 自動執行的最大金額 (USDC)，超過此金額需要確認

**安全注意事項**:
1. ⚠️ `.env` 文件絕對不能 commit 去 git
2. 先用小額測試 (0.1 USDC)
3. 確保 `.gitignore` 包含 `.env`
4. 私鑰只儲存在本地

## Instructions

### 1. 初始化設定

第一次使用時:

1. 創建 `.env` 文件（參考 `.env.sample`）
2. 填入你的私鑰和錢包地址
3. 設定 `MAX_AUTO_AMOUNT`（建議 1.0 USDC）
4. 執行測試腳本確認連接:
   ```bash
   python scripts/check_balance.py
   ```

### 2. 搜尋市場

當用戶想搜尋 Polymarket 市場時:

1. 使用 `scripts/search_markets.py` 搜尋市場:
   ```bash
   python scripts/search_markets.py "關鍵字"
   ```

2. 腳本會返回:
   - 市場 ID
   - 市場標題
   - 當前賠率
   - 交易量
   - 結束時間

3. 顯示結果給用戶，讓他們選擇要交易的市場

### 3. 查看持倉

當用戶想查看當前持倉時:

1. 執行 `scripts/get_positions.py`:
   ```bash
   python scripts/get_positions.py
   ```

2. 顯示:
   - 所有 active positions
   - 每個 position 的當前價值
   - 盈虧情況
   - 總持倉價值

### 4. 執行交易

**重要**: 交易前必須檢查金額!

#### 執行流程:

1. **準備交易**:
   - 確認市場 ID
   - 確認買入/賣出方向 (Yes/No)
   - 確認金額 (USDC)

2. **檢查金額**:
   ```python
   amount = float(交易金額)
   max_auto = float(os.getenv('MAX_AUTO_AMOUNT', '1.0'))

   if amount > max_auto:
       # 需要用戶確認
       顯示交易詳情並詢問確認
   ```

3. **執行交易**:
   ```bash
   python scripts/execute_trade.py \
     --market-id "市場ID" \
     --side "yes" \
     --amount "0.5"
   ```

4. **返回結果**:
   - 交易 hash
   - 交易狀態
   - Gas fee
   - 預期獲得的 shares

#### 交易確認範例:

**小額交易 (≤ MAX_AUTO_AMOUNT)**:
```
執行交易: 買入 0.5 USDC "市場名稱" - Yes
預計獲得: ~0.8 shares
Gas fee: ~0.01 USDC
正在執行...
```

**大額交易 (> MAX_AUTO_AMOUNT)**:
```
⚠️ 交易金額 5 USDC 超過自動執行限額 1 USDC

交易詳情:
- 市場: "Will Bitcoin reach $100k in 2025?"
- 方向: Yes
- 金額: 5 USDC
- 當前賠率: 0.65
- 預計獲得: ~7.7 shares
- 預計 Gas: ~0.02 USDC

是否確認執行此交易? (yes/no)
```

### 5. 錯誤處理

常見錯誤及處理:

1. **餘額不足**:
   - 檢查 USDC 餘額: `python scripts/check_balance.py`
   - 提示用戶充值

2. **私鑰錯誤**:
   - 確認 `.env` 文件中的私鑰格式正確
   - 私鑰應該是 64 字符的 hex string (不含 0x prefix)

3. **市場不存在**:
   - 重新搜尋市場
   - 確認市場 ID 正確

4. **Gas fee 過高**:
   - 等待 Polygon 網絡 gas 降低
   - 顯示當前 gas price

5. **交易失敗**:
   - 顯示完整錯誤訊息
   - 檢查市場是否還在交易中
   - 確認 approval 是否充足

## Workflow Summary

```
用戶請求
    │
    ├─ 搜尋市場? → search_markets.py → 顯示結果
    │
    ├─ 查看持倉? → get_positions.py → 顯示持倉
    │
    └─ 執行交易?
        │
        ├─ 檢查金額
        │   │
        │   ├─ ≤ MAX_AUTO_AMOUNT → 直接執行
        │   │
        │   └─ > MAX_AUTO_AMOUNT → 詢問確認 → 執行
        │
        └─ execute_trade.py → 返回結果
```

## Examples

### Example 1: 搜尋並買入市場

**用戶請求**:
```
幫我搜尋關於 "Trump 2024" 的 Polymarket 市場，然後買入 0.2 USDC 的 Yes
```

**你會執行**:

1. 搜尋市場:
   ```bash
   python scripts/search_markets.py "Trump 2024"
   ```

2. 顯示結果並讓用戶選擇:
   ```
   找到 3 個相關市場:

   1. [ID: abc123] Will Trump win 2024?
      - 當前 Yes: 0.52 | No: 0.48
      - 交易量: $2.5M
      - 結束: 2024-11-06

   2. [ID: def456] Trump vs Biden 2024
      ...
   ```

3. 用戶選擇市場 1

4. 執行交易 (0.2 USDC ≤ 1.0 自動執行):
   ```bash
   python scripts/execute_trade.py \
     --market-id "abc123" \
     --side "yes" \
     --amount "0.2"
   ```

5. 返回結果:
   ```
   ✅ 交易成功!
   - Tx Hash: 0x789...
   - 買入: 0.2 USDC
   - 獲得: 0.38 shares (Yes @ 0.52)
   - Gas: 0.008 USDC
   - 區塊: #12345678
   ```

### Example 2: 查看持倉

**用戶請求**:
```
顯示我在 Polymarket 上的所有持倉
```

**你會執行**:

1. 查詢持倉:
   ```bash
   python scripts/get_positions.py
   ```

2. 顯示結果:
   ```
   📊 你的 Polymarket 持倉:

   1. Will Bitcoin reach $100k in 2025? - Yes
      - 持有: 10.5 shares
      - 成本: 6.5 USDC
      - 當前價值: 7.8 USDC (0.74/share)
      - 盈虧: +1.3 USDC (+20%)

   2. Trump wins 2024 - No
      - 持有: 5.2 shares
      - 成本: 2.8 USDC
      - 當前價值: 2.4 USDC (0.46/share)
      - 盈虧: -0.4 USDC (-14%)

   總持倉價值: 10.2 USDC
   總成本: 9.3 USDC
   總盈虧: +0.9 USDC (+9.7%)
   ```

### Example 3: 大額交易需要確認

**用戶請求**:
```
買入 5 USDC 的 "Bitcoin $100k" Yes position
```

**你會執行**:

1. 檢查金額: 5 > 1.0 (MAX_AUTO_AMOUNT)

2. 顯示確認提示:
   ```
   ⚠️ 大額交易確認

   你即將執行以下交易:
   - 市場: "Will Bitcoin reach $100k in 2025?"
   - 方向: Yes
   - 金額: 5 USDC
   - 當前賠率: 0.74
   - 預計獲得: ~6.76 shares
   - 如果 Yes 贏: 回報 6.76 USDC (盈利 1.76 USDC)
   - 如果 No 贏: 損失 5 USDC
   - 預計 Gas: ~0.015 USDC

   錢包餘額: 12.3 USDC
   交易後餘額: ~7.3 USDC

   是否確認執行? (yes/no)
   ```

3. 等待用戶輸入 "yes"

4. 執行交易並返回結果

### Example 4: 市場分析後交易

**用戶請求**:
```
分析一下 "US recession 2024" 的市場情況，如果值得的話買入 0.5 USDC
```

**你會執行**:

1. 搜尋市場:
   ```bash
   python scripts/search_markets.py "US recession 2024"
   ```

2. 分析市場數據:
   ```
   市場: "Will US enter recession in 2024?"

   當前賠率:
   - Yes: 0.35 (35%)
   - No: 0.65 (65%)

   交易量: $1.2M (流動性充足)

   市場趨勢:
   - 24小時: Yes +5%
   - 7天: Yes +12%

   分析:
   市場認為衰退機會較低 (35%)，但最近趨勢顯示
   Yes 方賠率在上升，可能有新的經濟數據影響。
   如果你認為衰退機會被低估，Yes position 有潛在價值。
   ```

3. 提供建議:
   ```
   建議: 可以考慮小額買入 Yes position
   - 當前賠率 0.35 提供了不錯的潛在回報 (2.86x)
   - 交易量充足，容易退出
   - 風險: 0.5 USDC

   是否執行 0.5 USDC 買入? (會自動執行，金額在限額內)
   ```

4. 如用戶同意，執行交易

## Supporting Files

詳細技術文檔:
- [API_REFERENCE.md](API_REFERENCE.md) - Polymarket API 詳細說明
- [TRADING_STRATEGIES.md](TRADING_STRATEGIES.md) - 交易策略和最佳實踐

## Security Notes

1. **私鑰安全**:
   - 從不在代碼中顯示私鑰
   - 從不 log 私鑰
   - 確保 `.env` 在 `.gitignore` 中

2. **交易確認**:
   - 大額交易必須確認
   - 顯示完整交易詳情
   - 包含預期盈虧

3. **錯誤訊息**:
   - 不要暴露敏感訊息
   - 提供有用的錯誤提示
   - Log 錯誤但不 log 私鑰

## Troubleshooting

**Skill 沒有被觸發?**
- 確認關鍵字: polymarket, trade, bet, prediction market
- 嘗試明確說 "use polymarket trader skill"

**交易失敗?**
- 檢查餘額: `python scripts/check_balance.py`
- 檢查 gas: 可能需要更多 MATIC
- 檢查市場狀態: 市場可能已關閉

**無法連接錢包?**
- 確認 `.env` 文件存在
- 確認私鑰格式正確
- 確認 RPC URL 可用

---

**Version**: 1.0.0
**Last Updated**: 2025-12-03
**Author**: Created with meta-skill
